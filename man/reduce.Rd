% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduce.R
\name{reduce}
\alias{reduce}
\alias{reduce.array}
\title{Reduce operation for einops expressions}
\usage{
reduce(x, expr, func, ...)

\method{reduce}{array}(x, expr, func, ...)
}
\arguments{
\item{x}{object to reduce (array, matrix, etc.)}

\item{expr}{character string with einops expression (e.g., "h w c -> h w")}

\item{func}{reduction function to apply (e.g., sum, mean, max)}

\item{...}{additional arguments passed to the reduction function}
}
\value{
reduced object with dimensions according to output pattern
}
\description{
Reduces tensor dimensions according to einops notation. This function
applies a reduction function (like sum, mean, max) along specified dimensions
while potentially reshaping the tensor according to the einops pattern.
}
\examples{
# 2D reduction
arr <- array(1:12, dim = c(3, 4))
reduce(arr, "h w -> h", sum)  # Sum along width
reduce(arr, "h w -> w", sum)  # Sum along height
reduce(arr, "h w ->", sum)    # Sum to scalar

# 3D reduction with grouping
arr3d <- array(1:24, dim = c(2, 3, 4))
reduce(arr3d, "b h w -> h w", sum)      # Sum along batch
reduce(arr3d, "(b h) w -> w", sum)      # Group batch-height, sum

# Using ellipsis
reduce(arr3d, "... w -> ...", sum)      # Sum last dimension
}

{
    "name": "einops for R devcontainer",
    "shutdownAction": "none",
    "build": {
        "context": "..",
        "dockerfile": "../Dockerfile"
    },
    "runArgs": [
        // to use GPUs in container uncomment next line
        // "--gpus=all",
        "--name=einops-for-r-devbox"
    ]
}
